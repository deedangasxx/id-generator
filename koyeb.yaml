name: essences-x-session-generator
service:
  app:
    image: Dockerfile
    ports:
      - port: 8080
        http: /
    env:
      - name: PORT
        value: "8080"
    resources:
      cpu: 1000m
      memory: 512M
